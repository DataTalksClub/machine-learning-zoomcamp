# Evaluation lessons 05–09 screenshot rollout

Worker scope: `cohorts/2026/04-evaluation/05-roc.md` through
`09-explore-more.md`.

The rubric cutoff is 7/12, with the hard gates from
`zoomcamp-ops/template/illustration-rubric.md`: an image must add a specific
learning point, match its caption, and remain readable. Exact plots, code,
commands, URLs, numbers, and notebook/UI states use deterministic crops or
exports. Conceptual diagrams may use imagegen only after source inspection;
all originals are preserved. Duplicate `TPR_FPR.png` references share one
disposition.

Capability: this worker has the built-in `imagegen` skill and used it only for
bounded conceptual illustrations after deterministic crop/source inspection.

## Dispositions

- `TPR_FPR.png` — **keep**, score 12/12. This clean 1028×472 confusion-table diagram directly teaches the TPR/FPR formulas and is referenced twice in `05-roc.md`; it is already crisp, has no camera, browser/Zoom chrome, cursor, watermark, or unrelated overlay, and is not a redundant screenshot. Both references remain unchanged.
- `06-auc-02-auc-values.jpg` — **crop/replace**, score 11/12. The five ROC/AUC comparisons teach how curve shape maps to 0.5, 0.6, 0.8, 0.9, and 1.0; the original had a color-wheel overlay, camera tile, handwriting, and low readability. Cropped at `(x=110, y=0, w=394, h=350)` for source inspection, then regenerated with imagegen as `06-auc-02-auc-values-imagegen.png`; all five curves, values, and GOOD/GREAT/POOR labels were checked. Original preserved.
- `06-auc-05-auc-interpretation.jpg` — **crop/replace**, score 10/12. The score-ranking example explains the positive-vs-negative comparison behind AUC, but the source was a blurry whiteboard frame with a camera tile, color-wheel overlay, black border, and handwritten text. Cropped at `(x=14, y=0, w=476, h=350)` for source inspection, then regenerated with imagegen as `06-auc-05-auc-interpretation-imagegen.png`; NO CHURN/CHURN, NEGATIVE/POSITIVE groups, source scores, selected 0.50/0.60 pair, and callout were checked. Original preserved.
- `05-roc-01-tpr-fpr-vs-threshold.jpg` — **crop/replace**, score 12/12. The plot uniquely shows how both TPR and FPR fall as the classification threshold increases. The 372×248 source was already a clean plot with no overlays, so it was deterministically re-exported at 2× with Lanczos scaling and light sharpening as `05-roc-01-tpr-fpr-vs-threshold-cropped.png`; curve geometry, axes, ticks, and legend were checked. Original preserved.
- `05-roc-02-random-model-tpr-fpr.jpg` — **crop/replace**, score 11/12. The nearly overlapping descending TPR/FPR lines show that a random model cannot distinguish churners from non-churners. The 372×248 source was already a clean plot, so it was deterministically re-exported at 2× with Lanczos scaling and light sharpening as `05-roc-02-random-model-tpr-fpr-cropped.png`; both curves and axes were checked. Original preserved.
- `05-roc-03-ideal-model-tpr-fpr.jpg` — **crop/replace**, score 12/12. The plot uniquely shows the ideal model keeping TPR at 1.0 until the separating threshold while FPR falls to zero. The 372×248 source was already a clean plot, so it was deterministically re-exported at 2× with Lanczos scaling and light sharpening as `05-roc-03-ideal-model-tpr-fpr-cropped.png`; both curves, the threshold shape, axes, and legend were checked. Original preserved.
- `05-roc-04-model-vs-ideal-tpr-fpr.jpg` — **crop/replace**, score 12/12. The overlaid curves make the gap between the learned model and ideal reference explicit. The 372×248 source was deterministically re-exported at 2× with a small white border, Lanczos scaling, and light sharpening as `05-roc-04-model-vs-ideal-tpr-fpr-cropped.png`; all four curves, legend, and axes were checked. Original preserved.
- `05-roc-05-roc-curve-manual.jpg` — **crop/replace**, score 12/12. This plot is the lesson’s central visual evidence for comparing the model ROC curve with the random diagonal baseline. The 330×317 source was deterministically re-exported at 2× with a small white border, Lanczos scaling, and light sharpening as `05-roc-05-roc-curve-manual-cropped.png`; model/random lines, FPR/TPR axes, and legend were checked. Original preserved.
- `05-roc-06-roc-curve-sklearn.jpg` — **crop/replace**, score 12/12. This matching plot demonstrates that scikit-learn’s `roc_curve` produces the same model-vs-random ROC shape. The 330×317 source was deterministically re-exported at 2× with a small white border, Lanczos scaling, and light sharpening as `05-roc-06-roc-curve-sklearn-cropped.png`; model/random lines, FPR/TPR axes, and legend were checked. Original preserved.
- `06-auc-01-roc-curve-area.jpg` — **remove**, score 5/12 and hard-gated out. The capture is a near-duplicate of the preceding lesson’s model-vs-random ROC plot, contributes no new visual evidence, and does not visibly mark the area promised by its caption. The Markdown reference and redundant source were removed; the remaining AUC explanation and dedicated AUC-values illustration carry the concept.
- `06-auc-03-auc-notebook.jpg` — **crop/replace**, score 10/12. The notebook state provides exact evidence that `auc(fpr, tpr)` and `auc(df_scores.fpr, df_scores.tpr)` agree at `0.843850505725819`. Cropped at `(x=0, y=68, w=577, h=292)` from the 598×360 source and deterministically exported at 2× as `06-auc-03-auc-notebook-cropped.png`; exact headings, calls, and outputs were checked. Original preserved.
- `06-auc-04-roc-auc-score.jpg` — **crop/replace**, score 10/12. The notebook state demonstrates that `roc_auc_score(y_val, y_pred)` returns the same `0.843850505725819` as the explicit AUC calculation. Cropped at `(x=0, y=68, w=577, h=292)` from the 598×360 source and deterministically exported at 2× as `06-auc-04-roc-auc-score-cropped.png`; import, call, exact result, and nearby section transition were checked. Original preserved.
- `06-auc-06-auc-simulation.jpg` — **crop/replace**, score 10/12. The notebook state provides exact evidence for the Monte Carlo interpretation: 100000 random positive/negative pairs and result `0.8444`. Cropped at `(x=0, y=40, w=505, h=280)` from the 598×360 source to exclude the camera tile and notebook chrome, then exported at 2× as `06-auc-06-auc-simulation-cropped.png`; arrays, loop, comparison, and output were checked. Original preserved.
- `06-auc-07-auc-simulation-numpy.jpg` — **remove**, score 2/12 and hard-gated out. The surrounding lesson and caption require the vectorized NumPy simulation (`n=50000`, seeded `np.random.randint`, result `0.84646`), but the source capture shows the earlier Python loop (`n=100000`, result `0.8434`) and an unrelated typed cell. Cropping cannot repair the source/caption mismatch, so the Markdown reference and source were removed; the exact vectorized code remains in prose.
- `07-cross-validation-02-train-predict.jpg` — **crop/replace**, score 9/12. The capture adds notebook evidence for applying `predict` and initializing `KFold` with `n_splits=10`, `shuffle=True`, and `random_state=1`, but its original caption overstated what was visible. Cropped at `(x=0, y=40, w=505, h=185)` from the 598×360 source to remove browser/camera chrome and unrelated `pip` output, then exported at 2× as `07-cross-validation-02-train-predict-cropped.png`; the caption was corrected to match the visible content. Original preserved.
- `07-cross-validation-03-split-generator.jpg` — **crop/replace**, score 11/12. The exact notebook state shows `kfold.split(df_full_train)`, the `(5070, 564)` train/validation sizes, full-train length `5634`, and the `iloc` slices. Cropped at `(x=0, y=40, w=505, h=300)` from the 598×360 source to remove browser/camera chrome, then exported at 2× as `07-cross-validation-03-split-generator-cropped.png`; code, values, and row-splitting relationship were checked. Original preserved.
- `07-cross-validation-04-tuning-loop.jpg` — **crop/replace**, score 10/12. The notebook state shows the five-fold loop over all seven `C` values, train/validation slicing, AUC scoring, and mean/std print expression. Cropped at `(x=0, y=52, w=505, h=285)` from the 598×360 source to remove browser/camera chrome and a preceding-cell fragment, then exported at 2× as `07-cross-validation-04-tuning-loop-cropped.png`; loop structure and exact parameter values were checked. The long print line remains source-edge truncated. Original preserved.
