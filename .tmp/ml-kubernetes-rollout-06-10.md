# Kubernetes lessons 06–10 screenshot rollout

Capability: this worker has the `imagegen` skill and read its instructions. Exact code, command, URL, plot, numeric-output, and live-UI screenshots use deterministic crops/preparation. Imagegen is reserved for the bounded explanatory architecture illustration in lesson 09.

## Lesson 06

- `06-kubernetes-simple-service-01-ping-app.jpg` → `06-kubernetes-simple-service-01-ping-app-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact `ping.py` and Pipenv terminal content while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: the Flask ping app and installing gunicorn.
- `06-kubernetes-simple-service-02-build-ping.jpg` → `06-kubernetes-simple-service-02-build-ping-cropped.png`: deterministic stitched crop (terminal `446x270+55+24`, build output `596x53+0+294`), Lanczos 2× upscale and light sharpening. Preserves the exact Docker command and build/tag output, removes the camera tile and unrelated editor chrome. Original preserved. Teaching point: building and tagging the `ping:v001` image. The source command itself is truncated at `-p 96`; that source limitation is preserved rather than guessed.
- `06-kubernetes-simple-service-03-cluster-info.jpg` → `06-kubernetes-simple-service-03-cluster-info-cropped.png`: deterministic crop `447x274+55+31`, Lanczos 2× upscale and light sharpening. Preserves the exact `kubectl cluster-info`, service table, and empty-cluster output while removing the camera tile and most browser chrome. Original preserved. Teaching point: checking the fresh kind cluster and default service.
- `06-kubernetes-simple-service-04-deployment-yaml.jpg` → `06-kubernetes-simple-service-04-deployment-yaml-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact YAML and hand-written label relationships while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: matching deployment selectors and pod labels.
- `06-kubernetes-simple-service-05-kind-load.jpg` → `06-kubernetes-simple-service-05-kind-load-cropped.png`: deterministic crop `447x310+54+0`, Lanczos 2× upscale and light sharpening. Preserves the exact kind image-load error/retry, pod status, and port-forward output while removing the camera tile and surrounding editor chrome. Original preserved. Teaching point: loading a local image into kind and forwarding to the running pod. A small native pointer remains on the terminal's output bar; removing it safely would risk the exact output.
- `06-kubernetes-simple-service-06-service-yaml.jpg` → `06-kubernetes-simple-service-06-service-yaml-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact service YAML and hand-written annotations while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: the service name and selector that route requests to matching pods.
- `06-kubernetes-simple-service-07-service-lb.jpg` → `06-kubernetes-simple-service-07-service-lb-cropped.png`: deterministic crop `445x316+56+31`, Lanczos 2× upscale and light sharpening. Preserves the exact ClusterIP/LoadBalancer tables and `<pending>` external IP while removing the camera tile and browser/editor frame. Original preserved. Teaching point: changing the service type and seeing the local load balancer remain pending. A small native pointer remains in the blank terminal area.

## Lesson 07

- `07-kubernetes-tf-serving-01-kube-config.jpg` → `07-kubernetes-tf-serving-01-kube-config-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact kube-config tree and deployment template while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: organizing Kubernetes manifests and identifying the deployment template.
- `07-kubernetes-tf-serving-02-model-deployment.jpg` → `07-kubernetes-tf-serving-02-model-deployment-cropped.png`: deterministic crop `447x319+54+28`, Lanczos 2× upscale and light sharpening. Preserves the exact model image load, file listing, and deployment creation output while removing the camera tile and editor chrome. Original preserved. Teaching point: loading the TF-Serving image into kind and applying its deployment. A small native pointer remains near the terminal prompt.
- `07-kubernetes-tf-serving-03-model-test.jpg` → `07-kubernetes-tf-serving-03-model-test-cropped.png`: deterministic crop `347x319+155+28`, Lanczos 2× upscale and light sharpening. Preserves the exact port-forward failure, TensorFlow runtime output, and prediction dictionary while removing the camera tile and unrelated editor chrome. Original preserved. Teaching point: testing the model deployment through the forwarded port and `gateway.py`. The terminal source itself truncates some long lines at its window edges; no values were guessed or regenerated.
- `07-kubernetes-tf-serving-04-model-service.jpg` → `07-kubernetes-tf-serving-04-model-service-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact ClusterIP service manifest and selector/port values while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: putting an internal service in front of the TF-Serving pods.
- `07-kubernetes-tf-serving-05-gateway-deployment.jpg` → `07-kubernetes-tf-serving-05-gateway-deployment-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact visible gateway deployment fields and `TF_SERVING_HOST` setting while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: routing the gateway to the internal model service. The source capture truncates the long DNS value at the right edge; it was not guessed or regenerated.
- `07-kubernetes-tf-serving-06-telnet.jpg` → `07-kubernetes-tf-serving-06-telnet-cropped.png`: deterministic crop `447x317+54+30`, Lanczos 2× upscale and light sharpening. Preserves the exact telnet installation, DNS resolution, connection, and TensorFlow Serving greeting while removing the camera tile and editor chrome. Original preserved. Teaching point: verifying in-cluster DNS and port connectivity to TF-Serving.
- `07-kubernetes-tf-serving-07-gateway-service.jpg` → `07-kubernetes-tf-serving-07-gateway-service-cropped.png`: deterministic crop `447x317+54+30`, Lanczos 2× upscale and light sharpening. Preserves the exact service table, pending external IPs, and port-forward output while removing the camera tile and editor chrome. Original preserved. Teaching point: exposing the gateway through a LoadBalancer service and testing it locally.

## Lesson 08

- `08-eks-01-eksctl-install.jpg` → `08-eks-01-eksctl-install-cropped.png`: deterministic crop `447x317+54+30`, Lanczos 2× upscale and light sharpening. Preserves the exact `eksctl` download/extraction commands and course-directory listing while removing the camera tile and editor chrome. Original preserved. Teaching point: installing `eksctl` and entering the Kubernetes project directory.
- `08-eks-02-eks-config.jpg` → `08-eks-02-eks-config-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact EKS cluster and node-group YAML while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: configuring the EKS cluster name, region, instance type, and capacity.
- `08-eks-03-ecr-push.jpg` → `08-eks-03-ecr-push-cropped.png`: deterministic stitched crop (top `503x95+0+0`, lower `596x252+0+95`), Lanczos 2× upscale and light sharpening. Preserves the exact account, registry, image-tag, and ECR variable commands while removing the camera tile; the camera covered only the top-right editor chrome. Original preserved. Teaching point: mapping local Docker image tags to ECR remote tags.
- `08-eks-04-create-cluster.jpg` → `08-eks-04-create-cluster-cropped.png`: deterministic crop `450x317+53+30`, Lanczos 2× upscale and light sharpening. Preserves the exact `eksctl create cluster` command and availability-zone/subnet output while removing the camera tile and editor chrome. Original preserved. Teaching point: creating the EKS cluster from `eks-config.yaml`. Several long subnet lines remain clipped at the source terminal edge; no values were guessed.
- `08-eks-05-external-ip.jpg` → `08-eks-05-external-ip-cropped.png`: deterministic crop `450x317+53+30`, Lanczos 2× upscale and light sharpening. Preserves the exact gateway service table, external DNS name, and port while removing the camera tile and editor chrome. Original preserved. Teaching point: observing the EKS LoadBalancer external address. The source has a native terminal selection highlight over the DNS row and wraps/truncates some columns; preserving exact UI output takes priority over painting it out.
